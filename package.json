{
  "name": "tomcat-engine-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "npm run test:bridge && npm run test:platform",
    "test:bridge": "node --test bridge/*.test.mjs",
    "test:platform": "node --experimental-transform-types --test src/platform/*.test.ts"
  },
  "dependencies": {"react": "latest", "react-dom": "latest"},
  "devDependencies": {"@types/node": "latest", "@types/react": "latest", "@types/react-dom": "latest", "@vitejs/plugin-react": "latest", "vite": "latest", "typescript": "latest"}
}
